name: Email Step Workflow

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: 24f1001993@ds.study.iitm.ac.in
        run: echo "Hello, world!"
